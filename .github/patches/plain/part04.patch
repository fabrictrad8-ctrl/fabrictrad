-  try {
-    const response = await fetch(url);
-    const blob = await response.blob();
-    return new Promise((resolve, reject) => {
-      const reader = new FileReader();
-      reader.onloadend = () => resolve(reader.result as string);
-      reader.onerror = reject;
-      reader.readAsDataURL(blob);
-    });
-  } catch {
-    return url;
-  }
-}
-
-type Step = 'select' | 'preview' | 'result';
-type PhotoSource = 'model' | 'upload' | 'camera';
-
-interface DrapeSettings {
-  opacity: number;
-  blend: 'multiply' | 'overlay' | 'screen' | 'normal';
-  scale: number;
-  posX: number;
-  posY: number;
-  rotation: number;
+const GARMENT_STYLES = [
+  { id: 'saree', label: 'Saree', description: 'six-yard saree with a natural pallu and realistic pleats' },
+  { id: 'lehenga', label: 'Lehenga', description: 'full lehenga skirt with coordinated blouse and dupatta' },
+  { id: 'kurta', label: 'Kurta', description: 'tailored straight kurta with natural textile fall' },
+  { id: 'shirt', label: 'Shirt', description: 'premium long-sleeve shirt with realistic seams and collar' },
+  { id: 'dress', label: 'Dress', description: 'modern midi dress with clean tailoring and fabric movement' },
+  { id: 'dupatta', label: 'Dupatta', description: 'draped dupatta layered over the existing outfit' },
+] as const;
+
+const FIT_OPTIONS = ['Relaxed', 'Regular', 'Tailored'] as const;
+const MAX_UPLOAD_BYTES = 8 * 1024 * 1024;
+
+type SourceMode = 'model' | 'upload' | 'camera';
+
+function fileToDataUrl(file: File) {
+  return new Promise<string>((resolve, reject) => {
+    const reader = new FileReader();
+    reader.onload = () => resolve(String(reader.result));
+    reader.onerror = () => reject(new Error('Unable to read this image.'));
+    reader.readAsDataURL(file);
+  });
 }
 
 export default function FabricDrapeViewer() {
-  const [step, setStep] = useState<Step>('select');
-  const [selectedFabricId, setSelectedFabricId] = useState(PRODUCT_FABRIC_IMAGES[0].id);
-  const [selectedPoseId, setSelectedPoseId] = useState(MODEL_POSES[0].id);
-  const [uploadedPhoto, setUploadedPhoto] = useState<string | null>(null);
-  const [photoSource, setPhotoSource] = useState<PhotoSource>('model');
-  const [isGenerating, setIsGenerating] = useState(false);
-  const [showResult, setShowResult] = useState(false);
-  const [aiAnalysis, setAiAnalysis] = useState<string>('');
+  const { product } = useProduct();
+  const { user } = useAuth();
+  const { resolvedTheme } = useAppPreferences();
+  const [sourceMode, setSourceMode] = useState<SourceMode>('model');
+  const [selectedModel, setSelectedModel] = useState(MODEL_OPTIONS[0].id);
+  const [selectedStyle, setSelectedStyle] = useState(GARMENT_STYLES[0].id);
+  const [fit, setFit] = useState<(typeof FIT_OPTIONS)[number]>('Regular');
+  const [personImage, setPersonImage] = useState<string | null>(null);
   const [generatedImage, setGeneratedImage] = useState<string | null>(null);
-  const [usageCount, setUsageCount] = useState(0);
-  const [showPaywall, setShowPaywall] = useState(false);
+  const [analysis, setAnalysis] = useState('');
+  const [compare, setCompare] = useState(54);
+  const [fabricOpacity, setFabricOpacity] = useState(72);
+  const [fabricScale, setFabricScale] = useState(118);
+  const [generating, setGenerating] = useState(false);
+  const [error, setError] = useState('');
   const [cameraActive, setCameraActive] = useState(false);
-  const [cameraError, setCameraError] = useState('');
-  const [settings, setSettings] = useState<DrapeSettings>({
-    opacity: 72,
-    blend: 'multiply',
-    scale: 110,
-    posX: 0,
-    posY: 0,
-    rotation: 0,
-  });
-  const [isDragging, setIsDragging] = useState(false);
-  const [dragStart, setDragStart] = useState({ x: 0, y: 0 });
-  const photoInputRef = useRef<HTMLInputElement>(null);
+  const uploadRef = useRef<HTMLInputElement>(null);
   const videoRef = useRef<HTMLVideoElement>(null);
   const canvasRef = useRef<HTMLCanvasElement>(null);
   const streamRef = useRef<MediaStream | null>(null);
 
-  useEffect(() => {
-    const usage = getDrapeUsage();
-    const today = new Date().toDateString();
-    setUsageCount(usage.date === today ? usage.count : 0);
-  }, []);
+  const currentModel = useMemo(
+    () => MODEL_OPTIONS.find((item) => item.id === selectedModel) || MODEL_OPTIONS[0],
+    [selectedModel]
+  );
+  const currentStyle = useMemo(
+    () => GARMENT_STYLES.find((item) => item.id === selectedStyle) || GARMENT_STYLES[0],
+    [selectedStyle]
+  );
+  const basePersonImage = personImage || currentModel.image;
+  const fabricImage = product.images?.[0] || product.image;
 
   useEffect(() => {
     return () => {
-      if (streamRef.current) {
-        streamRef.current.getTracks().forEach((t) => t.stop());
-      }
+      streamRef.current?.getTracks().forEach((track) => track.stop());
     };
   }, []);
 
-  const selectedFabric =
-    PRODUCT_FABRIC_IMAGES.find((f) => f.id === selectedFabricId) || PRODUCT_FABRIC_IMAGES[0];
-  const selectedPose = MODEL_POSES.find((p) => p.id === selectedPoseId) || MODEL_POSES[0];
-  const modelSrc = uploadedPhoto || selectedPose.image;
-  const freeRemaining = Math.max(0, FREE_QUOTA - usageCount);
-
-  const handlePhotoUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
-    const file = e.target.files?.[0];
-    if (file) {
-      const url = URL.createObjectURL(file);
-      setUploadedPhoto(url);
-      setPhotoSource('upload');
-      setCameraActive(false);
-    }
+  const stopCamera = () => {
+    streamRef.current?.getTracks().forEach((track) => track.stop());
+    streamRef.current = null;
+    setCameraActive(false);
   };
 
-  const handleStartCamera = async () => {
-    setCameraError('');
+  const startCamera = async () => {
+    setError('');
     try {
+      if (!navigator.mediaDevices?.getUserMedia) throw new Error('Camera is not supported on this device.');
       const stream = await navigator.mediaDevices.getUserMedia({
-        video: { facingMode: 'user', width: { ideal: 640 }, height: { ideal: 480 } },
+        video: { facingMode: 'user', width: { ideal: 900 }, height: { ideal: 1200 } },
+        audio: false,
       });
       streamRef.current = stream;
-      if (videoRef.current) {
-        videoRef.current.srcObject = stream;
-        videoRef.current.play();
-      }
+      setSourceMode('camera');
       setCameraActive(true);
-      setPhotoSource('camera');
-    } catch {
-      setCameraError('Camera access denied. Please allow camera permission and try again.');
+      requestAnimationFrame(() => {
+        if (videoRef.current) {
+          videoRef.current.srcObject = stream;
+          void videoRef.current.play();
+        }
+      });
+    } catch (cameraError) {
+      setError(cameraError instanceof Error ? cameraError.message : 'Camera permission was not granted.');
     }
   };
 
-  const handleCapturePhoto = () => {
-    if (!videoRef.current || !canvasRef.current) return;
+  const captureCamera = () => {
     const video = videoRef.current;
     const canvas = canvasRef.current;
+    if (!video || !canvas || !video.videoWidth) return;
     canvas.width = video.videoWidth;
     canvas.height = video.videoHeight;
-    const ctx = canvas.getContext('2d');
-    if (!ctx) return;
-    ctx.translate(canvas.width, 0);
-    ctx.scale(-1, 1);
-    ctx.drawImage(video, 0, 0);
-    const dataUrl = canvas.toDataURL('image/jpeg', 0.9);
-    setUploadedPhoto(dataUrl);
-    if (streamRef.current) {
-      streamRef.current.getTracks().forEach((t) => t.stop());
-      streamRef.current = null;
-    }
-    setCameraActive(false);
+    const context = canvas.getContext('2d');
+    if (!context) return;
+    context.translate(canvas.width, 0);
+    context.scale(-1, 1);
+    context.drawImage(video, 0, 0, canvas.width, canvas.height);
+    setPersonImage(canvas.toDataURL('image/jpeg', 0.9));
+    setGeneratedImage(null);
+    stopCamera();
   };
 
-  const handleStopCamera = () => {
-    if (streamRef.current) {
-      streamRef.current.getTracks().forEach((t) => t.stop());
-      streamRef.current = null;
+  const handleUpload = async (event: React.ChangeEvent<HTMLInputElement>) => {
+    const file = event.target.files?.[0];
+    if (!file) return;
+    setError('');
+    if (!['image/jpeg', 'image/png', 'image/webp'].includes(file.type)) {
+      setError('Upload a JPG, PNG or WebP image.');
+      return;
+    }
+    if (file.size > MAX_UPLOAD_BYTES) {
+      setError('The model photo must be smaller than 8 MB.');
+      return;
+    }
+    try {
+      setPersonImage(await fileToDataUrl(file));
+      setSourceMode('upload');
+      setGeneratedImage(null);
+      stopCamera();
+    } catch (uploadError) {
+      setError(uploadError instanceof Error ? uploadError.message : 'Unable to read this image.');
+    } finally {
+      event.target.value = '';
     }
-    setCameraActive(false);
   };
 
-  const handleMouseDown = useCallback(
-    (e: React.MouseEvent) => {
-      e.preventDefault();
-      setIsDragging(true);
-      setDragStart({ x: e.clientX - settings.posX, y: e.clientY - settings.posY });
-    },
-    [settings.posX, settings.posY]
-  );
-
-  const handleMouseMove = useCallback(
-    (e: React.MouseEvent) => {
-      if (!isDragging) return;
-      setSettings((s) => ({ ...s, posX: e.clientX - dragStart.x, posY: e.clientY - dragStart.y }));
-    },
-    [isDragging, dragStart]
-  );
-
-  const handleMouseUp = useCallback(() => setIsDragging(false), []);
-
-  const handleTouchStart = useCallback(
-    (e: React.TouchEvent) => {
-      const touch = e.touches[0];
-      setIsDragging(true);
-      setDragStart({ x: touch.clientX - settings.posX, y: touch.clientY - settings.posY });
-    },
-    [settings.posX, settings.posY]
-  );
-
-  const handleTouchMove = useCallback(
-    (e: React.TouchEvent) => {
-      if (!isDragging) return;
-      const touch = e.touches[0];
-      setSettings((s) => ({
-        ...s,
-        posX: touch.clientX - dragStart.x,
-        posY: touch.clientY - dragStart.y,
-      }));
-    },
-    [isDragging, dragStart]
-  );
-
-  const handleTouchEnd = useCallback(() => setIsDragging(false), []);
-
-  const handleGenerate = async (bypassPaywall = false) => {
-    if (!bypassPaywall && freeRemaining <= 0) {
-      setShowPaywall(true);
+  const generateDrape = async () => {
+    setError('');
+    setAnalysis('');
+    if (!user) {
+      setError('Sign in as a buyer to create an AI drape preview.');
       return;
     }
-    setIsGenerating(true);
-    setAiAnalysis('');
-    setGeneratedImage(null);
-
+    setGenerating(true);
     try {
-      // Convert fabric image to base64
-      const fabricBase64 = await imageUrlToBase64(selectedFabric.src);
-      // Convert model/selfie image to base64
-      const modelBase64 = await imageUrlToBase64(modelSrc);
-
       const response = await fetch('/api/ai/drape-on', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({
-          fabricImage: fabricBase64,
-          modelImage: modelBase64,
-          fabricName: selectedFabric.name,
-          styleName:
-            photoSource === 'model' ? selectedPose.description : 'the person in the uploaded photo',
-          settings,
+          fabricImage,
+          modelImage: basePersonImage,
+          fabricName: `${product.name} (${product.gsm || 'unknown'} GSM, ${product.work || 'textile'})`,
+          styleName: `${fit.toLowerCase()} fit ${currentStyle.description}`,
         }),
       });
-      const data = await response.json();
-
-      if (!response.ok || data.error) {
-        throw new Error(data.error || 'AI drape generation failed');
+      const data = (await response.json().catch(() => ({}))) as {
+        image?: string;
+        analysis?: string;
+        error?: string;
+      };
+      if (!response.ok || !data.image) {
+        throw new Error(data.error || 'The drape preview could not be 