# Cloudflare build verification

This temporary file triggers the full type-check, lint, Next.js build, authentication source and route smoke tests, and OpenNext Cloudflare build against the corrected main branch.
